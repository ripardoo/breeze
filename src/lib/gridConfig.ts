/**
 * Shared grid configuration for the dashboard layout.
 * Centralized here so Dashboard, findWidgetSlot, and Topbar stay in sync.
 * Can be extended to support responsive breakpoints or user preferences later.
 */
export const GRID_COLS = 32;
export const GRID_ROWS = 18;
export const GRID_GAP = 8;
export const GRID_PADDING = 8;
export const DEFAULT_WIDGET_W = 2;
export const DEFAULT_WIDGET_H = 2;
